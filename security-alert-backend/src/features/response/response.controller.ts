import { Request, Response } from 'express';
export async function postAction(req: Request, res: Response) { res.json({ ok: true }); }
export async function postResult(req: Request, res: Response) { res.json({ ok: true }); }
