# Development environment example
# PostgreSQL (frontend)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=socconnect_frontend
POSTGRES_USER=socfront
POSTGRES_PASSWORD=changeme
POSTGRES_URL=postgresql://socfront:changeme@localhost:5432/socconnect_frontend

# ClickHouse (backend analytical store)
CLICKHOUSE_URL=https://ysn5zxsmja.ap-south-1.aws.clickhouse.cloud:8443
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=4mLednVx~QWR~

# Triage/model webhook (set to the ngrok endpoint you provided)
TRIAGE_URL=https://6e06e665c655.ngrok-free.app/triage
# Controls whether scalar fields are included as form fields (1 = include)
TRIAGE_INCLUDE_SCALARS=1
# Concurrency for outbound triage requests
TRIAGE_CONCURRENCY=5
# Retry/backoff settings for triage client
TRIAGE_MAX_RETRIES=3
TRIAGE_BACKOFF_MS=250
TRIAGE_TIMEOUT_MS=10000
# Number of triage payloads to keep in the in-memory debug store (app.ts)
TRIAGE_STORE_MAX=50

# MinIO (object store)
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=alerts-raw

# Similarity service (HNSW/embedding service)
SIMILARITY_URL=http://localhost:8000

# App
PORT=3002

# Notes: copy to .env and adjust credentials before running services
